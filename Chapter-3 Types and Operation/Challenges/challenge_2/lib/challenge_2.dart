import 'package:characters/characters.dart';
int main(){
  // const family = '👨‍👩‍👦‍👦';
 
  // print( family.characters.length);

  // didnt did this challenge because it is very tedious work to find the respective flag and it's code in the windows computer in my opinion and also the program seems to be very straightforward...
  // maybe will do this in sometime in future..............
  return 0;
}